import React from 'react';
import NavBar from '../../Shared/Navbar/NavBar';

const Header = () => {
    return (
        <div>
            <NavBar></NavBar>
        </div>
    );
};

export default Header;